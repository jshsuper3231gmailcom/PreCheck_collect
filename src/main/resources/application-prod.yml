spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    # TODO: 실제 운영 PostgreSQL host:port/db, 계정으로 교체
    url: jdbc:postgresql://prod-db-host:5432/precheck
    username: precheck
    password: precheck

precheck:
  collect:
    schedule-file-path: /home/precheck/cfg/PreCheck_CollectLogs_Schedule.conf
    file-charset: EUC-KR
  sftp:
    port: 22
    username: prod_user
    password: prod_pass
